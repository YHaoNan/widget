{
  "name": "widget",
  "version": "1.0.0",
  "description": "Add some dynamic widgets to your desktop.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "LILPIG",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.2.1"
  },
  "dependencies": {
    "electron-reload": "^1.5.0"
  }
}
